= render "new_weight"

= content_tag :div, "", id: 'WeightChart', data: {weights: @weights}

= render "weight_table"

aside.export.align-right
  p #{link_to "Download csv", weights_path(format: "csv")}
